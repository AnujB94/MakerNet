{
  "name": "@makernet/db",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "node --check scripts/migrate.mjs",
    "check": "node scripts/migrate.mjs check",
    "migrate": "node --env-file-if-exists=../../apps/web/.env.local scripts/migrate.mjs migrate",
    "verify": "node --env-file-if-exists=../../apps/web/.env.local scripts/migrate.mjs verify"
  },
  "dependencies": {
    "pg": "8.23.0"
  }
}
